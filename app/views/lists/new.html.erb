<div class="container">
  <h1>Add a new list:</h1>
  <%= simple_form_for @list do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit "Add your list", class: "btn btn-primary text-muted" %>
  <% end %>
</div>
